@{
    ViewData["Title"] = "Mail Gönderildi";
}

<div class="container py-5 mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">

            <div class="card border-0 shadow-lg rounded-4 overflow-hidden text-center bg-body">

                <div style="height: 10px; background: var(--primary-gradient);"></div>

                <div class="card-body p-5">

                    <div class="mb-4 position-relative d-inline-block">
                        <div class="position-absolute top-50 start-50 translate-middle bg-primary opacity-10 rounded-circle"
                             style="width: 100px; height: 100px; animation: pulse 2s infinite;"></div>

                        <div class="bg-body-tertiary p-3 rounded-circle shadow-sm position-relative">
                            <i class="fas fa-envelope-open-text fa-3x text-primary"></i>
                        </div>
                    </div>

                    <h3 class="fw-bold mb-3 text-body">Posta Yolda! 🚀</h3>

                    <p class="text-muted mb-4">
                        Şifre sıfırlama talimatlarını e-posta adresine gönderdik. Gelen kutunu kontrol edip linke tıklaman yeterli.
                    </p>

                    <div class="alert alert-warning border-0 bg-warning bg-opacity-10 rounded-3 text-start small mb-4">
                        <div class="d-flex gap-3">
                            <i class="fas fa-exclamation-triangle text-warning fs-5 mt-1"></i>
                            <div>
                                <strong>Maili göremiyor musun?</strong><br>
                                Lütfen <b>"Gereksiz"</b> veya <b>"Spam"</b> klasörünü kontrol etmeyi unutma.
                            </div>
                        </div>
                    </div>

                    <div class="d-grid gap-2">
                        <a asp-action="Login" class="btn btn-primary rounded-pill fw-bold py-2 shadow-sm"
                           style="background: var(--primary-gradient); border:none;">
                            Giriş Ekranına Dön
                        </a>

                        <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary rounded-pill fw-bold py-2">
                            Ana Sayfaya Git
                        </a>
                    </div>

                    <hr class="my-4 opacity-10">

                    <p class="small text-muted mb-0">
                        Hâlâ mail gelmedi mi?
                        <a asp-action="ForgotPassword" class="text-primary fw-bold text-decoration-none">
                            Tekrar Gönder
                        </a>
                    </p>

                </div>
            </div>

        </div>
    </div>
</div>

<style>
    @@keyframes pulse {
        0% {
            transform: translate(-50%, -50%) scale(0.9);
            opacity: 0.3;
        }

        50% {
            transform: translate(-50%, -50%) scale(1.2);
            opacity: 0.1;
        }

        100% {
            transform: translate(-50%, -50%) scale(0.9);
            opacity: 0.3;
        }
    }
</style>